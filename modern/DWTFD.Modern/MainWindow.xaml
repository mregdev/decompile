<Window x:Class="DWTFD.Modern.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="DWTFD — Windows Temizlik Aracı" Icon="app.ico" Width="700" Height="590"
        MinWidth="700" MaxWidth="700" MinHeight="590" MaxHeight="590" WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        Background="#0B1020" FontFamily="Segoe UI" Foreground="#F3F6FC">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="38" ResizeBorderThickness="0" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="Card" Color="#172137"/>
        <SolidColorBrush x:Key="Muted" Color="#9AABC3"/>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#62E4D0"/><Setter Property="Foreground" Value="#092229"/>
            <Setter Property="FontSize" Value="14"/><Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="22,12"/><Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="Body" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Body" Property="Opacity" Value="0.83"/></Trigger>
                    <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Body" Property="Opacity" Value="0.35"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="QuietButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#2A3954"/><Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="Toggle" TargetType="CheckBox">
            <Setter Property="Width" Value="46"/><Setter Property="Height" Value="26"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="CheckBox">
                <Grid Width="46" Height="26"><Border x:Name="Track" Background="#3A4961" CornerRadius="13"/>
                    <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="White" HorizontalAlignment="Left" Margin="4,0,0,0"/>
                </Grid>
                <ControlTemplate.Triggers><Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Track" Property="Background" Value="#62E4D0"/>
                    <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                    <Setter TargetName="Thumb" Property="Margin" Value="0,0,4,0"/>
                </Trigger></ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="ChromeButton" TargetType="Button">
            <Setter Property="Width" Value="46"/><Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="Transparent"/><Setter Property="Foreground" Value="#9AABC3"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="FontSize" Value="16"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="Body" Background="{TemplateBinding Background}">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter Property="Foreground" Value="White"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
            <Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#24354A"/></Trigger></Style.Triggers>
        </Style>
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource ChromeButton}">
            <Style.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="#B94C59"/></Trigger></Style.Triggers>
        </Style>
        <Style x:Key="TrackButton" TargetType="RepeatButton">
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="DarkThumb" TargetType="Thumb">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Thumb">
                <Border x:Name="Body" Background="#3E5C70" CornerRadius="4" Margin="1,0"/>
                <ControlTemplate.Triggers><Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Body" Property="Background" Value="#62E4D0"/></Trigger></ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="9"/><Setter Property="Background" Value="#0B1020"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ScrollBar">
                <Grid Width="{TemplateBinding Width}" Background="{TemplateBinding Background}">
                    <Track x:Name="PART_Track" IsDirectionReversed="True" Minimum="{TemplateBinding Minimum}" Maximum="{TemplateBinding Maximum}" ViewportSize="{TemplateBinding ViewportSize}" Value="{Binding Value, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                        <Track.DecreaseRepeatButton><RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource TrackButton}"/></Track.DecreaseRepeatButton>
                        <Track.Thumb><Thumb Style="{StaticResource DarkThumb}"/></Track.Thumb>
                        <Track.IncreaseRepeatButton><RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource TrackButton}"/></Track.IncreaseRepeatButton>
                    </Track>
                </Grid>
            </ControlTemplate></Setter.Value></Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="38"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#0B1020" BorderBrush="#22324A" BorderThickness="0,0,0,1">
            <Grid>
                <Image Source="app.png" Width="20" Height="20" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="13,0,0,0" IsHitTestVisible="False"/>
                <Button Content="×" HorizontalAlignment="Right" Style="{StaticResource CloseButton}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Click="CloseButton_Click" AutomationProperties.Name="Kapat"/>
            </Grid>
        </Border>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="22,12,22,10">
            <UniformGrid Columns="3" Margin="0,0,0,12">
                <Border Background="{StaticResource Card}" CornerRadius="13" Padding="17,10" Margin="0,0,8,0">
                    <StackPanel><TextBlock x:Name="SizeLabel" Text="TARANAN ALAN" Foreground="{StaticResource Muted}" FontSize="11" FontWeight="SemiBold"/>
                        <TextBlock x:Name="SizeValue" Text="—" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/></StackPanel>
                </Border>
                <Border Background="{StaticResource Card}" CornerRadius="13" Padding="17,10" Margin="4,0,4,0">
                    <StackPanel><TextBlock x:Name="FileLabel" Text="BULUNAN DOSYA" Foreground="{StaticResource Muted}" FontSize="11" FontWeight="SemiBold"/>
                        <TextBlock x:Name="FileValue" Text="—" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/></StackPanel>
                </Border>
                <Border Background="{StaticResource Card}" CornerRadius="13" Padding="17,10" Margin="8,0,0,0">
                    <StackPanel><TextBlock Text="ATLANAN" Foreground="{StaticResource Muted}" FontSize="11" FontWeight="SemiBold"/>
                        <TextBlock x:Name="SkippedValue" Text="—" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/></StackPanel>
                </Border>
            </UniformGrid>

            <Border Background="{StaticResource Card}" CornerRadius="17" Padding="22,14" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Temizlenecek alanlar" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="Seçimini değiştirdiğinde yeniden tarama yapılır." Foreground="{StaticResource Muted}" FontSize="12" Margin="0,3,0,17"/>
                    <Grid Margin="0,0,0,14"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Kullanıcı geçici dosyaları" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Hesabının Temp klasöründeki dosyalar" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,2,0,0"/></StackPanel>
                        <CheckBox x:Name="UserTempToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource Toggle}" Checked="SelectionChanged" Unchecked="SelectionChanged" VerticalAlignment="Center" AutomationProperties.Name="Kullanıcı geçici dosyaları"/>
                    </Grid>
                    <Border Height="1" Background="#2A3954" Margin="0,0,0,14"/>
                    <Grid Margin="0,0,0,14"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Windows Temp" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Sistem geçici klasörü; bazı dosyalar kullanımda olabilir" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,2,0,0"/></StackPanel>
                        <CheckBox x:Name="WindowsTempToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource Toggle}" Checked="SelectionChanged" Unchecked="SelectionChanged" VerticalAlignment="Center" AutomationProperties.Name="Windows Temp"/>
                    </Grid>
                    <Border Height="1" Background="#2A3954" Margin="0,0,0,14"/>
                    <Grid Margin="0,0,0,14"><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Son açılanlar listesi" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Windows geçmişini sıfırlar; gerçek dosyaları silmez" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,2,0,0"/></StackPanel>
                        <CheckBox x:Name="RecentToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource Toggle}" Checked="SelectionChanged" Unchecked="SelectionChanged" VerticalAlignment="Center" AutomationProperties.Name="Son açılanlar listesi"/>
                    </Grid>
                    <Border Height="1" Background="#2A3954" Margin="0,0,0,14"/>
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel><TextBlock Text="Prefetch · gelişmiş" FontSize="14" FontWeight="SemiBold"/>
                            <TextBlock Text="Uygulama açılış önbelleği; temizlemek açılışı geçici yavaşlatabilir" Foreground="{StaticResource Muted}" FontSize="12" Margin="0,2,0,0" TextWrapping="Wrap"/></StackPanel>
                        <CheckBox x:Name="PrefetchToggle" Grid.Column="1" IsChecked="True" Style="{StaticResource Toggle}" Checked="SelectionChanged" Unchecked="SelectionChanged" VerticalAlignment="Center" AutomationProperties.Name="Prefetch gelişmiş"/>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
        </ScrollViewer>

            <Border Grid.Row="2" Background="{StaticResource Card}" CornerRadius="17" Padding="18,13" Margin="22,0,22,14">
                <StackPanel>
                    <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock x:Name="StatusText" Text="Hazır" FontSize="15" FontWeight="SemiBold"/>
                            <TextBlock x:Name="DetailText" Text="Başlamak için alanları seçip tarama yap." Foreground="{StaticResource Muted}" FontSize="12" TextWrapping="Wrap" Margin="0,3,16,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="CancelButton" Content="İptal" Style="{StaticResource QuietButton}" Visibility="Collapsed" Click="CancelButton_Click" Margin="0,0,8,0"/>
                            <Button x:Name="ScanButton" Content="Tara" Style="{StaticResource QuietButton}" Click="ScanButton_Click" Margin="0,0,8,0"/>
                            <Button x:Name="CleanButton" Content="Temizle" Style="{StaticResource ActionButton}" IsEnabled="False" Click="CleanButton_Click"/>
                        </StackPanel>
                    </Grid>
                    <ProgressBar x:Name="WorkProgress" Height="5" Background="#2A3954" Foreground="#62E4D0" BorderThickness="0" Margin="0,15,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Border>
    </Grid>
</Window>
